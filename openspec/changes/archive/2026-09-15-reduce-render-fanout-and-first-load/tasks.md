## 1. Baseline

- [x] 1.1 Зафиксировать базовые замеры на `npm run build && npm run start`: сценарий «клик по табу» и «добавить пожелание» в Profiler (Self time, «why did this render»), число font-запросов и число запросов `?_rsc=` в Network на первой загрузке главной — верификация: записанные значения до правок сохранены для сравнения
- [x] 1.2 Спайк: проверить, обновляется ли значение `useSearchParams` после нативного `window.history.replaceState` в Next 13.5 — верификация: ответ зафиксирован; при любом исходе используется схема из `design.md` (решение 1) без изменения плана

## 2. Вкладка FAQ без навигации

- [x] 2.1 Перевести вкладку на состояние: `useState` с начальным значением из `searchParams`, эффект-синхронизация с `urlTab` — верификация: `npm run build` проходит, страница не требует отказа от `Suspense`
- [x] 2.2 Заменить `router.replace` на `setTab` + `window.history.replaceState` со сборкой URL из `window.location`; убрать `useRouter` и `usePathname` из импортов и тела компонента — верификация: `npm run lint` и `npm run typecheck` проходят без предупреждений о неиспользуемых импортах
- [x] 2.3 Проверить сценарии на `npm run start`: клик по табу, переход по индикатору из шапки с уже открытой страницы FAQ (`Header.tsx:111`), прямая ссылка `/faq?tab=messages`, перезагрузка на активной вкладке — верификация: вкладка соответствует адресу во всех случаях, `?_rsc=` в Network при клике по табу отсутствует, пустая заглушка содержимого не появляется

## 3. Разбиение DataContext по доменам

- [x] 3.1 Создать в `lib/data-context.tsx` три контекста и три мемоизированных значения (`Wishes`, `Donations`, `Chats`) по `design.md`, решение 2 — верификация: `npm run typecheck` проходит, тип `DataStore` и имя `DataProvider` сохранены
- [x] 3.2 Добавить хуки `useWishes()`, `useDonations()`, `useChats()` и удалить `useData()` — верификация: `grep -rn "useData()" --include=*.tsx --include=*.ts` не находит вызовов вне определения
- [x] 3.3 Перевести `Header.tsx:30` и `app/(app)/faq/page.tsx:54` и `ChatThread.tsx:21` на `useChats()` — верификация: шапка, страница FAQ и лента переписки отображают непрочитанные и сообщения при `npm run start`
- [x] 3.4 Перевести `WishBoard.tsx:42` на `useWishes()` — верификация: добавление и редактирование пожелания работают при `npm run start`
- [x] 3.5 Перевести `DonateDialog.tsx:48` на `useDonations()` и `useWishes()` — верификация: шаг подтверждения отправляет взнос и создаёт пожелание, `declinedUserIds` учитывает историю
- [x] 3.6 Перевести `AdminTable.tsx:79` на `useDonations()` — верификация: таблица, журнал и переключение статуса подарка работают при `npm run start`
- [x] 3.7 Обновить мок `@/lib/data-context` в `components/features/DonateDialog.test.tsx:52-69`: заменить `useData` на `useWishes` и `useDonations` — верификация: `npm test` проходит, оба теста `DonateDialog` зелёные
- [x] 3.8 Проверить изоляцию перерисовок на `npm run start`: добавить пожелание и убедиться, что `Header` и `ChatThread` не рендерятся повторно; отправить взнос и убедиться, что `WishBoard` не рендерится повторно — верификация: Profiler не показывает повторных рендеров этих компонентов

## 4. Мемоизация значений корневых провайдеров

- [x] 4.1 Завернуть `value` в `useMemo` в `lib/auth-context.tsx:47` — верификация: `npm run typecheck` проходит, вход и выход работают при `npm run start`
- [x] 4.2 Завернуть `value` в `useMemo` в `lib/theme-context.tsx:36` — верификация: переключение всех трёх тем и сохранение выбора работают при `npm run start`

## 5. Шрифты активной темы

- [x] 5.1 Выставить `preload: false` для `Bricolage_Grotesque`, `Manrope`, `Fraunces`, `Inter` в `app/layout.tsx:16-38`, оставив `preload: true` у `Nunito`; `subsets` не менять — верификация: `npm run build` проходит, в собранном HTML 0 ссылок `rel="preload" as="font"`
- [x] 5.2 Проверить первую загрузку на `npm run start`: число font-запросов при теме `warm` и при сохранённой теме `premium` — верификация: при `warm` запрашиваются только файлы `nunito` (latin+cyrillic), при `festival` только `bricolage` (latin) и `manrope` (latin+cyrillic), при `premium` только `fraunces` (latin) и `inter` (latin+cyrillic); загрузка только активной темы подтверждена составом `document.fonts` (loaded содержит исключительно семейства активной темы), сравнение с базовым замером из 1.1 (8 файлов всех семейств) в пользу нового. Замер выполняется в отдельном документе на каждую тему: переключение темы внутри одной страницы догружает чужие шрифты и давало ложные 8 запросов

## 6. Ленивый диалог участия (условный шаг)

- [x] 6.1 Сравнить замер первой загрузки главной после шагов 2-5 с бюджетом LCP — верификация: LCP главной 88–148 мс при бюджете 2,5 с (CLS 0,0081–0,0082 при бюджете 0,1); первая загрузка в бюджете, поэтому группа 6 пропускается целиком
- [x] 6.2 Подключить `DonateDialog` через `next/dynamic` с `ssr: false` и монтировать его только после первого открытия (`donateMounted`, без размонтирования) — пропущено: по решению 6.1 первая загрузка укладывается в бюджет LCP, условный шаг не выполняется
- [x] 6.3 Проверить первый клик «Поздравить / отправить средства» на `npm run start` — пропущено: по решению 6.1 первая загрузка укладывается в бюджет LCP, условный шаг не выполняется

## 7. Финальная проверка

- [x] 7.1 Запустить `npm run lint`, `npm run typecheck`, `npm test` — верификация: 0 ошибок, 7/7 тестов проходят
- [x] 7.2 Запустить `npm run build` — верификация: успешная сборка без предупреждений о `useSearchParams` и о `ssr: false`
- [x] 7.3 Свести замеры до/после по сценариям из 1.1 на `npm run start` — верификация: клик по табу 1 → 0 запросов `?_rsc=` без пустой заглушки; изоляция перерисовок подтверждена счётчиками (взнос не рендерит `Header`/`WishBoard`, пожелание не рендерит `Header`); первая загрузка запрашивает шрифты только активной темы; LCP и CLS в бюджете — все затронутые требования `openspec/specs/performance/spec.md` и `openspec/specs/support-chat/spec.md` подтверждены наблюдением
- [x] 7.4 Проверить, что не затронуто запрещённое: `React.memo` на `Header`/`WishBoard`/`HomePage` не добавлен, сбросы по `usePathname` в `WishBoard:49`, `DonateDialog:56`, `AdminTable:88` сохранены, `useAsyncAction` не изменён — верификация: `React.memo` отсутствует, `usePathname` присутствует в `WishBoard.tsx:49`, `DonateDialog.tsx:57`, `AdminTable.tsx:88`, `lib/hooks/useAsyncAction.ts` не менялся
- [x] 7.5 Запустить `cmd /c "openspec validate reduce-render-fanout-and-first-load"` — верификация: изменение валидно до архивации
