## 1. Единое подключение шрифта

- [x] 1.1 В `app/layout.tsx` оставить только импорт и конфиг `Manrope` (`subsets: ['latin', 'cyrillic']`, `variable: '--font-manrope'`, `preload: true`), удалить `Nunito`, `Bricolage_Grotesque`, `Fraunces`, `Inter` и их переменные из `fontVariables` — верификация: в файле нет символов `nunito`, `bricolage`, `fraunces`, `inter`; `npm run typecheck` проходит
- [x] 1.2 В `lib/theme.ts` удалить поле `fonts` из интерфейса `ThemeMeta` и из записей `warm`, `festival`, `premium` — верификация: поиск `fonts` по `lib/theme.ts` не даёт совпадений; `npm run typecheck` проходит

## 2. Единые типографические токены

- [x] 2.1 В `app/globals.css` задать `--font-heading` и `--font-body` один раз как `var(--font-manrope), sans-serif`, удалить их переопределения из `[data-theme='festival']` и `[data-theme='premium']` — верификация: в файле ровно по одному объявлению каждой переменной; нет ссылок на `--font-nunito`, `--font-bricolage`, `--font-fraunces`, `--font-inter`
- [x] 2.2 Подтвердить, что `tailwind.config.ts` и классы `font-heading`/`font-body` не требуют правок — верификация: `npm run build` проходит, утилиты `font-heading`/`font-body` присутствуют в собранном CSS

## 3. Проверка поведения единого шрифта

- [x] 3.1 Проверить, что переключение всех трёх тем не меняет семейство шрифта заголовков и текста — верификация: в DevTools Computed `font-family` заголовка и абзаца одинакова для `warm`, `festival`, `premium` и соответствует `Manrope`
- [x] 3.2 Проверить отображение кириллицы единым шрифтом — верификация: русский заголовок и текст отрисованы `Manrope`; в списке использованных шрифтов нет резервного системного семейства
- [x] 3.3 Проверить состав запрашиваемых шрифтовых файлов — верификация: в Network запрашиваются только файлы `Manrope` (latin + cyrillic), запросов удалённых семейств нет
- [x] 3.4 Прогнать автоматические проверки проекта — верификация: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` завершаются без ошибок

## 4. Регрессия критических сценариев

- [x] 4.1 Проверить, что смена темы и восстановление сессии не скрывают содержимое и сохраняют требование о серверном контенте первой отрисовки — верификация: главная и календарь отображают контент до гидратации и после переключения темы без пустого экрана
- [x] 4.2 Проверить, что интерактив шапки (переключатель темы, меню пользователя) остаётся доступным после навигации между разделами — верификация: меню открываются и закрываются, прокрутка не блокируется
