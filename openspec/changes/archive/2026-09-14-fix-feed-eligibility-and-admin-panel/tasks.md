## 1. Общие хелперы

- [x] 1.1 В `lib/birthdays.ts` добавить `getTodayBirthdays(today, users)`, `hasBirthdayNotPassed(today, user)` и `getCongratulatableUsers(today, users)` (последний исключает текущего пользователя); проверить `npm run typecheck`
- [x] 1.2 В `lib/birthdays.ts` добавить хелпер назначения персонального цвета по списку пользователей (детерминированно, сортировка по `fullName`), переиспользуемый и карточкой именинника, и лентой; проверить, что для одного и того же списка порядок цветов совпадает

## 2. Лента поздравлений

- [x] 2.1 В `components/features/WishBoard.tsx` перевести ленту на одну доступную (`aria-hidden`) копию списка по умолчанию — статичная полоса без дублей; проверить, что при 1–2 пожеланиях каждая карточка отображается один раз
- [x] 2.2 Добавить измерение переполнения контейнера (ref + `ResizeObserver`) и включать режим зацикленной прокрутки только при переполнении; проверить, что при малом числе карточек дубликат не монтируется, а при большом — включается автопрокрутка
- [x] 2.3 В `app/globals.css` применять анимацию `wish-marquee` только под модификатором режима прокрутки и не дублировать содержимое при статике и `prefers-reduced-motion`; проверить, что при включённом reduced motion остаётся одна вручную прокручиваемая полоса
- [x] 2.4 Проверить паузу автопрокрутки при наведении и фокусе в режиме прокрутки; признак завершения — наведение и фокус останавливают движение, содержимое остаётся доступным

## 3. Единый цвет именинника

- [x] 3.1 В `components/features/BirthdayCard.tsx` принимать персональный цвет и использовать его для обводки при `isToday` вместо `ring-primary`; проверить, что обводка карточки совпадает с цветом карточек её поздравлений
- [x] 3.2 В `app/(app)/page.tsx` вычислять цвета сегодняшних именинников общим хелпером и передавать в `BirthdayCard`, а в `WishBoard` использовать тот же хелпер; проверить, что один сотрудник имеет одинаковый цвет в карточке и ленте, а разные — разные цвета

## 4. Доступность бесплатного пожелания

- [x] 4.1 В `components/features/WishBoard.tsx` ограничить список получателей формы сегодняшними именинниками через `getTodayBirthdays`; проверить, что в списке нет других сотрудников и самого пользователя
- [x] 4.2 Сделать контролы «Оставить пожелание» (hero-кнопка в `app/(app)/page.tsx` и кнопка доски) неактивными, когда сегодня именинников нет, с понятной подсказкой; проверить, что форма не открывается и пожелание не создаётся

## 5. Допустимые получатели денежного поздравления

- [x] 5.1 В `components/features/DonateDialog.tsx` заменить `recipientOptions` на `getCongratulatableUsers`; проверить, что сотрудники с прошедшим днём рождения отсутствуют в списке, а сегодняшние и будущие присутствуют, сам пользователь исключён

## 6. Тексты интерфейса

- [x] 6.1 Удалить утверждение «Участие добровольное.» из `components/layout/Footer.tsx`; проверить, что футер не содержит ни утверждения о добровольности, ни утверждения о суммах
- [x] 6.2 Обновить ответ FAQ про автора в `app/(app)/faq/page.tsx` на отображение «настоящее имя + ник в скобках» с примером `Фамилия Имя (ник)`; проверить, что полный адрес почты не раскрывается и формулировка совпадает с поведением доски

## 7. Админ-панель: дата рождения и статус подарка

- [x] 7.1 Добавить `giftSent: boolean` в тип `Donation` (`lib/types.ts`) и засидить значение в `lib/mock-data.ts`; проверить `npm run typecheck`
- [x] 7.2 Добавить `setGiftSent(userId, sent)` в `lib/data-store.ts` и пробросить в `lib/data-context.tsx`; проверить, что смена статуса отражается в состоянии сессии
- [x] 7.3 В `components/features/AdminTable.tsx` добавить колонку «Дата рождения» с форматированием через `parseIsoLocal`; проверить, что дата отображается для каждой строки
- [x] 7.4 В `components/features/AdminTable.tsx` добавить колонку «Подарок» со статусом «Выслано» / «Не выслано» и ручным переключением администратором; проверить, что переключение сразу отражается в таблице и недоступно сотруднику

## 8. Проверка и финализация

- [x] 8.1 Прогнать `npm run typecheck` и `npm run lint` без ошибок
- [x] 8.2 Прогнать ручные сценарии: именинник сегодня; именинников сегодня нет (заглушка прошедшего дня); мало пожеланий — статика; много пожеланий — прокрутка; reduced motion; попытка поздравить заранее бесплатно; список денежных получателей без прошедших дат; отметка статуса подарка
- [x] 8.3 Выполнить `openspec validate "fix-feed-eligibility-and-admin-panel"` и убедиться в отсутствии ошибок
