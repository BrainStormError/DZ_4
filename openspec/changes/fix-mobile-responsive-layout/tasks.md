## 1. Общие примитивы наложения и корневая защита

- [x] 1.1 Обновить `components/ui/dialog.tsx` (`DialogContent`): `w-[calc(100%-1.5rem)]`, `max-h-[90dvh]`, `overflow-y-auto`, `overscroll-contain` в базовом классе. Проверка: открыть диалоги участия/изменения суммы/изменения пожелания на 320×568 — диалог имеет боковые отступы, не выходит за вьюпорт, при нехватке высоты прокручивается и кнопки достижимы
- [x] 1.2 Обновить `components/ui/popover.tsx` и `components/ui/select.tsx`: `collisionPadding={8}`, у `SelectContent` — `max-w-[calc(100vw-2rem)]`, у `SelectItem`/`ItemText` — `min-w-0` и `truncate`. Проверка: на 320px открыть поповер выбора даты и селект получателя — списки внутри вьюпорта, длинные подписи усекаются с ellipsis, полное значение доступно через `title`
- [x] 1.3 Добавить защиту от горизонтального переполнения на `body` в `app/globals.css`. Проверка: на 320px и 360px ширина `document.documentElement.scrollWidth` не превышает ширину вьюпорта и горизонтальный скролл страницы отсутствует
- [x] 1.4 Добавить в `tailwind.config.ts` кастомный вариант `hoverable` через `addVariant('hoverable', '@media (hover: hover)')`. Проверка: `npm run typecheck` завершается без ошибок, в собранном CSS присутствует правило для `hoverable:hover:scale-[1.02]`

## 2. Главная страница

- [x] 2.1 В `app/(app)/page.tsx` заменить контейнер кнопок hero на `flex-col sm:flex-row`, кнопкам добавить `w-full sm:w-auto`, `whitespace-normal`, `h-auto min-h-11 py-2.5 px-5 sm:px-8`. Проверка: на 320px и 360px обе кнопки полностью видны внутри hero, текст не обрезается, на `sm+` раскладка остаётся строкой
- [x] 2.2 Добавить `scroll-mt-20` секции `#wish-board` в `app/(app)/page.tsx`. Проверка: активация «Оставить пожелание» приводит к заголовку доски, не скрытому под sticky-шапкой
- [x] 2.3 Заменить сетку блока «Скоро день рождения» на `grid-cols-2 md:grid-cols-3 lg:grid-cols-4` в `app/(app)/page.tsx`. Проверка: на 640px имена в карточках отображаются полностью без обрезки
- [x] 2.4 В `components/features/BirthdayCard.tsx` заменить `hover:scale-[1.02]` на `hoverable:hover:scale-[1.02]`. Проверка: на сенсорном эмуляторе касание карточки не оставляет применённого масштаба; на десктопе наведение увеличивает карточку

## 3. Календарь

- [x] 3.1 В `app/(app)/calendar/page.tsx` группе навигации задать `flex-wrap items-center justify-center gap-2`, стрелкам — `size="sm"` и `h-8 w-8 shrink-0`, у `h2` убрать `min-w-[160px]` и задать `text-lg sm:text-xl whitespace-nowrap`. Проверка: на 320px `scrollWidth` страницы равен ширине вьюпорта, все кнопки и заголовок видны
- [x] 3.2 В `app/(app)/calendar/page.tsx` ниже `sm` рендерить в ячейке компактную отметку дня рождения с полным перечнем именинников в `title` вместо усечённого имени; имена оставить на `sm+`. Проверка: на 320px ячейка с именинником показывает различимую отметку и не отображает обрезанное до одной буквы имя, полный список доступен из `title`

## 4. Динамическая высота вьюпорта

- [x] 4.1 Заменить `min-h-screen` на `min-h-[100dvh]` в `app/(app)/layout.tsx`. Проверка: на мобильном при показе/скрытии адресной строки высота не скачет и контент не обрезается
- [x] 4.2 Заменить `min-h-screen` на `min-h-[100dvh]` в `app/(auth)/login/page.tsx`. Проверка: экран входа остаётся вертикально центрированным без скачка высоты

## 5. Чат

- [x] 5.1 В `components/features/ChatThread.tsx` задать корню карточки `h-[70dvh] md:h-[600px]` и добавить `min-h-0` в `CardContent`, сохранив `min-h-0` во внутренней flex-цепочке. Проверка: при длинной переписке карточка сохраняет высоту, история прокручивается внутри, форма отправки остаётся внутри карточки и достижима

## 6. Таблица админа

- [x] 6.1 В `components/features/AdminTable.tsx` удалить внешний `div.overflow-x-auto` вокруг `Table`, задать ячейкам `px-2 sm:px-4`, скрыть колонки «Отдел» и «Дата рождения» через `hidden md:table-cell`, e-mail в ячейке сотрудника — `hidden sm:block`, подписи кнопок «Подарок»/«Изменить» — `hidden sm:inline` с добавлением `aria-label`. Проверка: на 640px и 320px страница не получает горизонтальный скролл, колонка «Действие» видна, прокрутка таблицы — в одном контейнере
- [x] 6.2 Прогнать `npx vitest run components/features/AdminTable.test.tsx`. Проверка: тесты таблицы и гейтинга редактирования проходят без изменений в тестовом файле

## 7. Мобильная навигация

- [x] 7.1 В `components/layout/Header.tsx` ссылкам мобильной навигации задать `min-h-[44px] py-2.5`, контейнеру — `snap-x snap-mandatory`, элементам — `snap-start`. Проверка: высота области нажатия каждого пункта не меньше 44px, при переполнении прокрутка выравнивается по пунктам
- [x] 7.2 Добавить визуальную подсказку о горизонтальной прокрутке мобильной навигации (градиент-маска с `pointer-events-none`), показываемую только при переполнении. Проверка: на 320px администратор видит признак продолжения и достигает пунктов «FAQ» и «Админ»; при помещающихся пунктах подсказка отсутствует

## 8. Лента пожеланий

- [x] 8.1 В `components/features/WishBoard.tsx` задать контейнеру ленты `snap-x snap-mandatory`, карточкам — `snap-start`, сохранив `overflow-x-auto` и ручную прокрутку без автопрокрутки. Проверка: при горизонтальной прокрутке карточки выравниваются по шагу и не остаются обрезанными у края, лента доступна с клавиатуры
- [x] 8.2 Добавить визуальную подсказку о горизонтальной прокрутке ленты, видимую только при переполнении. Проверка: при не помещающихся карточках подсказка видна, при помещающихся — отсутствует

## 9. Итоговая проверка

- [x] 9.1 Прогнать `npm run lint`, `npm run typecheck` и `npm test`. Проверка: все команды завершаются без ошибок
- [x] 9.2 Выполнить ручную проверку на 320px, 360px и 640px: главная, календарь, админ-таблица, чат, диалоги участия и изменения суммы, мобильное меню, лента пожеланий. Проверка: горизонтальный скролл страницы отсутствует, контент и контролы полностью видны и достижимы, состояния загрузки/ошибок не изменились
