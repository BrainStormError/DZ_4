## 1. Модель данных и хранилище

- [x] 1.1 Добавить `updateWish(id, text)` в `useDataStore` (`lib/data-store.ts`): обновляет текст пожелания по id, не меняя автора, получателя и `createdAt`; проверка — `npm run typecheck` проходит без ошибок
- [x] 1.2 Проксировать `updateWish` через `lib/data-context.tsx`; проверка — `useData()` возвращает метод, `npm run typecheck` проходит
- [x] 1.3 Добавить необязательное поле `readByAdmin?: boolean` в `ChatMessage` (`lib/types.ts`); проверка — `npm run typecheck` проходит
- [x] 1.4 Добавить `markThreadRead(userEmail)` в `useDataStore`: помечает прочитанными все сообщения ветки с `isAdmin === false`; проверка — `npm run typecheck` проходит
- [x] 1.5 Проставить `readByAdmin: true` уже отвеченным веткам в `lib/mock-data.ts` (ветка Анны), оставив ветку без ответа непрочитанной; проверка — при входе администратором счётчик непрочитанных равен числу неотвеченных веток

## 2. Паритет формы участия

- [x] 2.1 Убрать ролевые ветвления шагов в `DonateDialog.tsx`: сценарий всегда `recipient → email → amount → message → confirm → success`; проверка — администратор и сотрудник проходят одинаковое число шагов
- [x] 2.2 Сделать тексты шагов и экрана результата нейтральными (вариант сотрудника) и вызывать `addWish` независимо от роли; проверка — после подтверждения администратором на доске появляется пожелание от его имени, сумма не отображается
- [x] 2.3 Проверить, что шаг «Поздравление» блокирует продолжение при пустом тексте и что экран результата одинаков для обеих ролей; проверка — ручной прогон сценария под обеими ролями

## 3. Редактирование пожеланий

- [x] 3.1 Добавить на карточку пожелания в `WishBoard.tsx` действие «Изменить», видимое только при `user.role === 'admin'`; проверка — у сотрудника элементов редактирования нет, у администратора есть
- [x] 3.2 Реализовать диалог редактирования с `Textarea`, сохранением через `updateWish` и блокировкой пустого текста; проверка — после сохранения на доске виден новый текст, автор и получатель не изменились

## 4. Раздел сообщений и непрочитанные

- [x] 4.1 В `app/(app)/faq/page.tsx` задать название вкладки по роли: «Сообщения» для администратора, «Написать админу» для сотрудника, и скорректировать описание карточки по роли; проверка — под обеими ролями заголовки соответствуют спецификации `support-chat`
- [x] 4.2 В `ChatThread.tsx` вызывать `markThreadRead` при выборе ветки администратором; проверка — после открытия ветки счётчик непрочитанных уменьшается
- [x] 4.3 Добавить бейдж непрочитанных на вкладку «Сообщения»; проверка — при наличии непрочитанных бейдж виден, при отсутствии — скрыт
- [x] 4.4 Сделать вкладки контролируемыми и открывать вкладку сообщений по `?tab=messages` из `window.location.search` в `useEffect`; проверка — переход на `/faq?tab=messages` открывает вкладку сообщений

## 5. Шапка и меню профиля

- [x] 5.1 Удалить неинтерактивный пункт отдела из меню профиля в `Header.tsx`; проверка — в меню остаются профиль, отдел как подпись отсутствует, нет ложных команд
- [x] 5.2 Добавить в `Header.tsx` индикатор непрочитанных сообщений только для администратора со ссылкой на `/faq?tab=messages`; проверка — индикатор виден администратору с непрочитанными и отсутствует у сотрудника и при нуле непрочитанных

## 6. Копирайт

- [x] 6.1 Удалить «Суммы сборов видны только администратору.» из `components/layout/Footer.tsx`, оставив «Участие добровольное.»; проверка — футер не содержит утверждения о суммах
- [x] 6.2 Удалить «Суммы сборов видит только администратор.» и «Тёплые слова важнее суммы.» из `app/(app)/page.tsx`; проверка — на главной нет упоминаний видимости сумм и фразы про тёплые слова

## 7. Интеграционная проверка

- [x] 7.1 Прогнать под администратором и сотрудником страницы `/`, `/calendar`, `/faq`, `/admin`: формы участия одинаковы, пожелание администратора появляется на доске, редактирование работает, вкладка «Сообщения» отвечает, индикатор непрочитанных снимается при открытии ветки; проверить консоль на отсутствие ошибок
- [x] 7.2 Запустить `npm run typecheck`, `npm run lint` и `npm run build`; проверить отсутствие ошибок и предупреждений о новых модулях
