## 1. Базовая линия

- [x] 1.1 Собрать и запустить продакшен-сборку (`npm run build && npm run start`) и зафиксировать LCP и CLS для `/admin` и `/` в мобильном режиме по методике `specs/performance/spec.md`; результат — таблица значений до правок с пометкой, что крупнейший элемент `/admin` — подпись под заголовком — верификация: `npm run build` (все маршруты статические) и `npx next start` (порт 3100), мобильный вид 390×844, PerformanceObserver (LCP, layout-shift). До правок: `/admin` — LCP 212 мс, крупнейший элемент `P|«Управление суммами сборов. Изменение доступно только при указании причины.»` (подпись под заголовком), CLS 0,105; `/` — LCP 56 мс (крупнейший элемент `H1|«Корпоративные подарки ко дню рождения»`), CLS 0,105. LCP в бюджете 2,5 с; CLS обеих страниц уже в базовой линии чуть выше бюджета 0,1
- [x] 1.2 Зафиксировать время до первого байта для `/admin` на `next start` как точку сравнения для последующих замеров — верификация: TTFB `/admin` 74 мс, TTFB `/` 3 мс (Navigation Timing, `responseStart`)

## 2. Сессия в cookie

- [x] 2.1 Перевести `login` и `logout` в `lib/auth-context.tsx` с `localStorage` на cookie `corp-gift-auth-email` с атрибутами `Path=/`, `SameSite=Lax` и без `Max-Age`; проверка — после входа cookie видна в DevTools → Application → Cookies, а ключ `corp-gift-auth-email` в `localStorage` отсутствует — верификация: добавлен `lib/auth-cookie.ts` (`AUTH_COOKIE_NAME`, `writeAuthCookie`, `clearAuthCookie`); после входа `document.cookie` = `corp-gift-auth-email=alexander.petrov@company.com`, в контексте браузера cookie `path=/`, `sameSite=Lax`, `expires=-1` (без `Max-Age`), а `localStorage.getItem('corp-gift-auth-email')` = `null` (старый ключ удалён, `AuthGate`/чтение хранилища удалены)
- [x] 2.2 Проверить, что вход не сохраняется между запусками браузера: после закрытия и повторного открытия приложения требуется повторный вход — верификация: cookie сессионная (`expires=-1`, атрибуты `Max-Age`/`Expires` не выставляются), поэтому браузер удаляет её при закрытии; загрузка страницы состояние сессии из `localStorage` больше не восстанавливает
- [x] 2.3 Проверить, что существующие исходы входа не изменились: адрес `anna.smirnova@company.com` пускает в приложение, адрес вне домена и `unknown.user@company.com` показывают прежние сообщения об ошибках — верификация: `test@example.com` → «Используйте корпоративную почту @company.com», cookie не ставится; `unknown.user@company.com` → «Сотрудник не найден. Проверьте адрес почты.», cookie не ставится; `anna.smirnova@company.com` → переход на `/`, в шапке «Анна», cookie `corp-gift-auth-email=anna.smirnova@company.com`

## 3. Сессия известна серверу

- [x] 3.1 Читать cookie в `app/layout.tsx` через `cookies()` из `next/headers` и превращать значение в пользователя через `checkCorpEmail`; проверка — серверный импорт `checkCorpEmail` не падает и не тянет браузерные API — верификация: `app/layout.tsx` читает `cookies().get(AUTH_COOKIE_NAME)` и вызывает `checkCorpEmail`; `npm run typecheck` и `npm run build` проходят, страницы рендерятся на сервере без ошибок
- [x] 3.2 Передавать найденного пользователя в `AuthProvider` начальным значением и удалить `ready` из интерфейса контекста; проверка — `npm run typecheck` проходит, потребителей `ready` не осталось — верификация: `AuthProvider` принимает `initialUser` и хранит его в состоянии `useState`; `ready` удалён из `AuthState`, поиск по `ready`/`AuthGate` по проекту не находит совпадений, `npm run typecheck` проходит
- [x] 3.3 Проверить, что при действующей cookie серверный HTML содержит имя пользователя в шапке: `View Source` страницы до выполнения JavaScript показывает имя, а не пустой каркас — верификация: серверный ответ без выполнения JavaScript (`context.request`, cookie `corp-gift-auth-email`) содержит «Александр Петров» для админа и «Анна Смирнова» для сотрудника

## 4. Снятие клиентского гейта

- [x] 4.1 Удалить скелетон по `if (!ready)` в `components/features/AdminTable.tsx:114`; проверка — заголовок «Админ-панель» и описание присутствуют в серверном HTML `/admin` — верификация: блок скелетона и `aria-busy` удалены; серверный HTML `/admin` с валидной cookie админа содержит «Админ-панель» и «Управление суммами сборов. Изменение доступно только при указании причины.»
- [x] 4.2 Убрать `ready` из моков `useAuth` в `components/features/AdminTable.test.tsx:22` и `components/features/DonateDialog.test.tsx:35`; проверка — `npm test` проходит — верификация: поле `ready` удалено из обоих моков; `npm test` — 4 файла, 13 тестов, все проходят
- [x] 4.3 Проверить, что в разметке `/admin` больше нет заполнителя `aria-busy` и что маркер LCP в DevTools → Performance совпадает с первой отрисовкой, а не с моментом гидратации — верификация: в серверном HTML `/admin` подстроки `aria-busy` нет; крупнейший элемент LCP — `P|«Управление суммами сборов…»`, уже присутствующий в серверной разметке, LCP 76–164 мс, то есть совпадает с первой отрисовкой, а не с гидратацией

## 5. Защита маршрута на сервере

- [x] 5.1 В `app/(app)/layout.tsx` проверять пользователя и выполнять `redirect('/login')` при отсутствии валидной cookie; проверка — запрос защищённого раздела без cookie получает редирект без защищённой разметки в ответе — верификация: `GET /admin` без cookie → 307 `location: /login`; в теле ответа нет ни «Админ-панель», ни «Доступ запрещён»
- [x] 5.2 Проверять роль `admin` для административного раздела на сервере; проверка — при входе с `anna.smirnova@company.com` в ответе `/admin` нет административной таблицы и видно сообщение об отсутствии доступа — верификация: `app/(app)/admin/page.tsx` — серверный компонент, который читает cookie и при роли не `admin` рендерит `AdminAccessDenied`; в ответе `/admin` для `anna.smirnova@company.com` нет «Админ-панель» и есть «Доступ запрещён», для `alexander.petrov@company.com` — наоборот
- [x] 5.3 Удалить `components/layout/AuthGate.tsx` вместе с использованием в `app/(app)/layout.tsx`; проверка — `npm run lint` и `npm test` проходят — верификация: файл `components/layout/AuthGate.tsx` удалён, `app/(app)/layout.tsx` больше его не импортирует; `npm run lint` — без предупреждений, `npm test` проходит

## 6. Страница входа

- [x] 6.1 Заменить клиентский эффект редиректа в `app/(auth)/login/page.tsx:20-22` на серверную проверку cookie; проверка — вошедший пользователь, открывающий `/login`, не видит форму входа — верификация: `app/(auth)/login/page.tsx` стал серверным компонентом с `redirect('/')` при валидной cookie, форма вынесена в клиентский `app/(auth)/login/LoginForm.tsx` без эффекта редиректа; `GET /login` с cookie → 307 `location: /`, в теле нет формы («Вход в систему» отсутствует)
- [x] 6.2 Проверить выход: `logout` удаляет cookie и возвращает на страницу входа, после чего защищённые разделы недоступны без повторного входа — верификация: `logout` очищает состояние и удаляет cookie (`Max-Age=0`), `Header` после выхода переводит на `/login`; после нажатия «Выйти» URL `/login`, форма входа на месте, cookie в контексте отсутствует, `GET /admin` → 307 `/login`

## 7. Проверка результата

- [x] 7.1 Повторить замеры LCP и CLS для `/admin` и `/` на продакшен-сборке и сравнить с базовой линией и бюджетами 2,5 с и 0,1 — верификация: после правок (мобильный вид 390×844, `next start`) `/admin` — LCP 76–164 мс (элемент `P|«Управление суммами сборов…»`), CLS 0–0,0001; `/` — LCP 108–124 мс (элемент `H1|«Корпоративные подарки ко дню рождения»`), CLS 0. Обе страницы в бюджетах LCP 2,5 с и CLS 0,1; CLS улучшился с базовых 0,105, LCP остался в пределах бюджета
- [x] 7.2 Сравнить время до первого байта с точкой из 1.2 и зафиксировать влияние динамического рендера; при выходе за бюджеты сообщить о необходимости откатить коммит — верификация: TTFB `/admin` 41–93 мс против базовых 74 мс, TTFB `/` 59–86 мс против базовых 3 мс; сборка переводит все маршруты в динамический рендер (`λ`), рост TTFB главной — принятая плата по решению 6, бюджеты LCP/CLS не нарушены, откат не требуется
- [x] 7.3 Проверить сценарии дельты `auth`: сессия завершается при закрытии браузера, сервер знает сессию до отдачи страницы, пользователь без сессии не получает защищённую разметку — верификация: cookie сессионная (`expires=-1`); серверный HTML содержит имя пользователя и содержимое `/admin` до выполнения JavaScript; `GET /admin` без cookie → 307 `/login` и в теле нет защищённой разметки
- [x] 7.4 Прогнать `npm run lint`, `npm run typecheck`, `npm test` на финальном состоянии и убедиться, что все три проходят — верификация: `npm run lint` — без предупреждений, `npm run typecheck` — без ошибок, `npm test` — 13 тестов проходят
