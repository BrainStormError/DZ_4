## 1. Правило показа пожеланий

- [x] 1.1 Убрать fallback на ближайшую прошедшую дату в `lib/birthdays.ts:31` (`getBoardDate` отбирает только сотрудников с днём рождения в текущую дату, неиспользуемое поле `BoardDate.date` удаляется) и перевести `components/features/WishBoard.tsx:53` на полученный список именинников; проверить, что `npm run typecheck` проходит без ошибок
- [x] 1.2 Переписать тексты пустого состояния `components/features/WishBoard.tsx:352-361`: при отсутствии именинников — пояснение, что пожелания доступны в день рождения; при именинниках без пожеланий — предложение оставить первое; проверить вручную на текущей дате без именинников, что доска остаётся на месте и не показывает карточек за прошедшую дату
- [x] 1.3 Добавить юнит-тест на правило показа (дата передаётся аргументом, без зависимости от даты запуска): пожелание сегодняшнего именинника отображается, пожелание за прошедшую дату — нет; проверить, что `npm test` проходит и тест падает при возврате fallback

## 2. Доска использует доступную ширину

- [x] 2.1 Дать карточке ленты растягиваться от текущего минимума (280px, с `sm` — 320px) до читаемого максимума и центрировать полосу, когда она не заполняет ряд (`components/features/WishBoard.tsx:329-351`); проверить на 1440px с одним пожеланием, что карточка растянута, а свободное место распределено по краям
- [x] 2.2 Проверить, что при гибких ширинах сохранены статичная полоса, ручная прокрутка, snap и подсказка о прокрутке: детектор переполнения `components/features/WishBoard.tsx:96-101` не показывает подсказку при 1–4 пожеланиях и показывает при 6+, а левые карточки остаются достижимыми при прокрутке на 320/360/1024/1440px

## 3. Подпись футера

- [x] 3.1 Перевести подпись футера в столбик с центрированием по умолчанию и вернуть одну строку с `sm` (`components/layout/Footer.tsx:8-12`); проверить на 320px, что строка описания не начинается с тире и иконка не рассинхронизирована по вертикали, а на 640px и выше подпись снова в одну строку

## 4. Ближайшие дни рождения

- [x] 4.1 Сменить раскладку сетки на `grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4` (`app/(app)/page.tsx:154`); проверить на 320/360/414px, что карточки идут друг под другом и имена не обрезаны, а на 640/1024/1440px раскладка остаётся прежней

## 5. Проверка изменения

- [x] 5.1 Прогнать `npm run lint`, `npm run typecheck` и `npm test` — все три команды завершаются без ошибок
- [x] 5.2 Ручная проверка на 320/360/414/640/1024/1440px с 1, 2, 4 и 6 пожеланиями, в день с именинниками и в день без них, включая предпросмотр даты администратором: нет горизонтального скролла страницы, имена в блоке ближайших именинников не обрезаны, лента не дублирует карточки
